todolist C++
# V1.0
c++ to do list terminal use

# V2.0
sqlite3 database to keep the tasks permanently

# V3.0
With QT6 real GUI app with sqlite3 to keep the tasks permanently, adding a logo + information to compile on macos tahoe with m-chip
